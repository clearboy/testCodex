# testCodex

This project contains a simple Node.js backend built with [Express](https://expressjs.com/).

## Getting Started

Install dependencies:

```bash
npm install
```

Start the server:

```bash
npm start
```

The server will listen on port `3000` by default and respond with a greeting on the root path.
